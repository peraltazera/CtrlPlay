 if(loading){
        return(
            <div className="min-h-20">
                <p className="text-lg">Loading...</p>
            </div>
        )
    }

    if(error){
        return(
            <div className="min-h-20">
                <p className="text-lg">{error}</p>
            </div>
        )
    }
